<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ms">
	<select id="getPostList" resultType="sharemarketDTO">
		SELECT bd.board_no as boardNo, bd.id, bd.title, bd.content, bd.hits, bd.wdate,
		cg.category_name as categoryName
		FROM board bd, category cg, sharemarket sm
		WHERE bd.board_no=sm.board_no
		AND sm.category_no=cg.category_no
		order by bd.board_no desc
	</select>
</mapper>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
















